=========
--Create React App--  (learning React, building a new single-page application in React)
- You’ll need to have Node >= 8.10 and npm >= 5.6 on your machine. To create a project, run:
    - npx create-react-app react_website
    - cd react_website
    - npm start
============
Change port to 8080, by add following in file /package.json
"scripts": {
    "start": "PORT=8080 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
===========
install bootstrap and react-router-dom
 - cd react_website
 - npm i bootstrap@5.0.0-alpha1
 - npm i react-router-dom

